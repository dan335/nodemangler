# Backlog

- [ ] Frame / Comment Nodes
- [ ] Dot / Reroute Nodes

## Panel system follow-ups (system shipped 2026-07; full plan in git history: `git show 77d98db:PLAN.md`)

- [ ] Per-leaf graph pan/zoom — duplicate graph-view panels currently share one GraphEditor viewport
- [ ] Persist secondary-window layouts — "set panel layout as default" saves the main-window tree only
- [ ] Overlays (node-drop from list, Tab search, ghost node, status toasts) render in the main window only; graph panels in secondary windows support pan/zoom/select/connect but not node-drop
