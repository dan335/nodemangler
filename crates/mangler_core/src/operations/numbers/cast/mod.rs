//! Type casting operations for converting between integer and decimal types.

/// Converts a value to an integer (truncating decimals).
pub mod to_integer;
/// Converts a value to a decimal (widening integers to f32).
pub mod to_decimal;
