way to tell in an input is invalid
do away with valid check.  anything can go into anything
add colorspace to add node
    way to disable input if not needed
    color space on add node relies on one input being a color or image
add mask to add node


* structure of app
    * delay updates if things are busy
    * make mangler lib runnable from exe
    * valid types
    * node for a whole graph.  use a graph inside a graph

* ui
    * zoom view panel
    * show any output in view panel not just main
    * panel layout
    * highlight nodes that are possible connections when creating connection

* saving/loading
    * show graph settings if no nodes are selected

* more operations


reference
    ui
        https://twitter.com/jamesm

    colors
        https://github.com/JiatLn/color-art


